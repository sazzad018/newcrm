# Database Configuration
# Replace with your actual PostgreSQL credentials
# Note: SSL is auto-disabled for localhost/127.0.0.x connections
# For remote databases, SSL certificate validation is automatically handled
DATABASE_URL=postgresql://username:password@localhost:5432/social_ads_db

# Session Secret (Generate a random string)
SESSION_SECRET=your-random-secret-key-here

# Environment
NODE_ENV=production

# Server Port (default: 5000)
PORT=5000
